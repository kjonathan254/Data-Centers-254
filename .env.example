# Database — Turso (production)
DATABASE_URL="libsql://dc254-your-org.turso.io"
TURSO_AUTH_TOKEN="your-turso-auth-token"

# Database — SQLite (local development only)
# DATABASE_URL="file:./db/custom.db"

# Google Analytics (optional — leave empty to disable)
NEXT_PUBLIC_GA_ID=""

# Resend — Contact form emails
RESEND_API_KEY="re_..."
# RESEND_FROM_EMAIL="noreply@datacentre254.com"  # optional, verified custom domain
