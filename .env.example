# Database — SQLite (local development)
DATABASE_URL="file:./db/custom.db"

# Database — Turso (production)
# DATABASE_URL="libsql://dc254-your-org.turso.io?authToken=your-turso-auth-token"

# Google Analytics (optional — leave empty to disable)
NEXT_PUBLIC_GA_ID=""
